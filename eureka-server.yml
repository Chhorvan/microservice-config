server:
  port: 8082
eureka:
  client:
    register-with-eureka: false # it will stop registering eure server itself with registry.
    fetch-registry: false # it will not allow the same server to use it for any common purpose.
